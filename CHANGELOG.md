# Changelog

## v1.0.0

- Introduced deterministic multi-stage scan pipeline and static reporting.
- Added secrets scope model: tracked + ignored-sensitive patterns.
- Added history scan status contract (`never_scanned`, `fresh`, `stale`).
- Added per-zone inventory counts and secrets scope pattern audit output.
